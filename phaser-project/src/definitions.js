var DEFAULT_FONT = '"Lucida Sans Unicode", "Lucida Grande", sans-serif';

var DEPTH_ENUM = {
  BACKGROUND : 0,
  ROAD : 50,
  CAR : 75,
  UI : 100
}

var TURN_ENUM = {
  LEFT : -1,
  FORWARD : 0,
  RIGHT : 1
}

var INPUT_ENUM = {
  KEY_LEFT : 0,
  KEY_RIGHT : 1,
  KEY_DOWN : 2
}
